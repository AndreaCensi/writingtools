%:section:common/statistics/sorting:Sorting vectors

\newcommand{\order}{\mathsf{order}}  % Order (or rank) of the elements of a vector.
%:nomenc:\order(x): Order (or rank) of the elements of a vector $x$.
%:sort:order
%:def:def:order

\newcommand{\sorted}{\mathsf{sorted}} % Sorted version of a vector
%:nomenc:\sorted(x): Sorted version of a vector $x$.
%:sort:sorted
%:def:def:sorted

\newcommand{\differ}{\vmath{differ}} %
%:nomenc:\differ(\reals,n):Subset of $\reals^n$ for which they are all different.
%:sort:differ
%:def:def:differ

\newcommand{\sortedSeq}{\vmath{sortedSeq}} %
%:nomenc:\sortedSeq(\setX,n):Subset of $\setX^n$ such that $x_i < x_{i+1}$. 
%:def:def:sortedSeq
%:sort:sortedSeq

\newcommand{\weaksortedSeq}{\vmath{weaksortedSeq}} %
%:nomenc:\weaksortedSeq(\setX,n):Subset of $\setX^n$ such that $x_i \leq x_{i+1}$. 
%:def:def:weaksortedSeq
%:sort:weaksortedSeq